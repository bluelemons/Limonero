<%= semantic_form_for @lote do |form| %>
  <%= error_message(@lote)%>

  <%= form.inputs :manzana, :loteo, :parcela, :partida, :ncuotas, :monto_cents, :monto_currency, :interes %>

  <%= form.buttons :commit %>
<% end %>
