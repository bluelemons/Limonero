<%= form_for(@nacionalidad) do |f| %>
  <% if @nacionalidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nacionalidad.errors.count, "error") %> prohibited this nacionalidad from being saved:</h2>

      <ul>
      <% @nacionalidad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
