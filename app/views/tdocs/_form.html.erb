<%= form_for(@tdoc) do |f| %>
  <% if @tdoc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tdoc.errors.count, "error") %> prohibited this tdoc from being saved:</h2>

      <ul>
      <% @tdoc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
