<%= form_for(@loteo) do |f| %>
  <% if @loteo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loteo.errors.count, "error") %> prohibited this loteo from being saved:</h2>

      <ul>
      <% @loteo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
