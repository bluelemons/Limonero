<%= form_for(@sexo) do |f| %>
  <% if @sexo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sexo.errors.count, "error") %> prohibited this sexo from being saved:</h2>

      <ul>
      <% @sexo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
