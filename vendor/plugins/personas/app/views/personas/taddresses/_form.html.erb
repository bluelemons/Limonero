<%= form_for(@taddress) do |f| %>
  <% if @taddress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taddress.errors.count, "error") %> prohibited this taddress from being saved:</h2>

      <ul>
      <% @taddress.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
